# OAK Depth Alignment Configuration
# Configuration used during this calibration session

depth_alignment:
  aligned_to_rgb: true
  output_width: 1280
  output_height: 720

stereo:
  mode: standard
  subpixel_enabled: true
  lr_check_enabled: true
  confidence_threshold: 200
  median_filter: 5

notes: "Sample session depth configuration"
